https://slides.docker.noverit.com

http://tutorial.kubernetes.noverit.com/

http://jpetazzo.github.io/assets/2015-07-01-deep-dive-into-docker-storage-drivers.html#1

 
docker help
docker help run
docker ps
docker ps -a
docker start name
docker logs name
docker inspect name ->> hostname --> container , 0.0.0.0 host machine

docker run -i -t --rm= true imagine /bin/bash    run(reau sa creez si sa rulez) -i(interacative) --rm=true ( sa sterg containerul dupce ce rulez daca ma loghez iar le am) imagine(nu o ai se downloadeaza)
                              cat .dockercfg (configuratie)
                              docker login https://.....
                                             docker search dupa repository

                                            

docker images (ne arata toate imaginile pe care le avem)
docker run -i -t --rm= true imagine /bin/bash
doker stop name
docker restart name
docker exec -i -t name /bin/bash ( to get inside to container)
docker version (sa vezi versiunea)
(one application per container) sa ferifici aplicatie : docker inspect,logs check the server log                             
docker help kill
docker kill -s 'SIGKILL'  nume (oprire fortata)